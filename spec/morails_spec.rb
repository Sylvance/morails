# frozen_string_literal: true

RSpec.describe Morails do
  it "has a version number" do
    expect(Morails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
